import React from 'react';
import ShortUrlForm from "./components/shortUrlForm";

const App: React.FC = () => {
    return (
        <div>
            <ShortUrlForm />
        </div>
    );
};

export default App;
